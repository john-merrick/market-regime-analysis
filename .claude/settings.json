{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git add -A && git diff-index --quiet HEAD || (git commit -m \"wip\" && git push) 2>/dev/null || true",
            "async": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
